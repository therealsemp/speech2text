<Window x:Class="speech2text.UI.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="speech2text"
        SizeToContent="WidthAndHeight"
        WindowStyle="ToolWindow"
        Topmost="True"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <StackPanel Margin="12" MinWidth="240">

        <!-- Top bar -->
        <DockPanel Margin="0,0,0,10">
            <TextBlock Text="speech2text" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <Button DockPanel.Dock="Right"
                    Content="—"
                    Command="{Binding MinimizeCommand}"
                    Padding="6,2"
                    BorderThickness="0"
                    Background="Transparent"
                    HorizontalAlignment="Right"/>
        </DockPanel>

        <!-- Status indicator -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Ellipse Width="10" Height="10" VerticalAlignment="Center" Margin="0,0,8,0">
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Setter Property="Fill" Value="#AAAAAA"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsRecording}" Value="True">
                                <Setter Property="Fill" Value="#E53935"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsTranscribing}" Value="True">
                                <Setter Property="Fill" Value="#FB8C00"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>
            <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" FontWeight="SemiBold"/>
        </StackPanel>

        <!-- Profile selector -->
        <TextBlock Text="Profile" Foreground="#888888" FontSize="11" Margin="0,0,0,2"/>
        <ComboBox ItemsSource="{Binding Profiles}"
                  SelectedItem="{Binding ActiveProfile}"
                  DisplayMemberPath="Name"
                  Margin="0,0,0,8"/>

        <!-- Audio device selector -->
        <TextBlock Text="Microphone" Foreground="#888888" FontSize="11" Margin="0,0,0,2"/>
        <ComboBox ItemsSource="{Binding AudioDevices}"
                  SelectedItem="{Binding SelectedDevice}"
                  DisplayMemberPath="Name"
                  Margin="0,0,0,10"/>

        <!-- Recording toggle -->
        <Button Content="{Binding RecordingButtonLabel}"
                Command="{Binding ToggleRecordingCommand}"
                Padding="8,4" Margin="0,0,0,8"
                HorizontalAlignment="Stretch"/>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal">
            <Button Content="Settings" Command="{Binding OpenSettingsCommand}" Padding="8,4" Margin="0,0,4,0"/>
            <Button Content="Close"    Command="{Binding CloseCommand}"        Padding="8,4"/>
        </StackPanel>

        <!-- Error panel -->
        <Border Background="#FFEBEE" CornerRadius="4" Padding="8,6" Margin="0,8,0,0"
                Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="{Binding ErrorMessage}"
                           TextWrapping="Wrap"
                           Foreground="#C62828"
                           FontSize="11"/>
                <Button Grid.Column="1"
                        Content="x"
                        Command="{Binding DismissErrorCommand}"
                        VerticalAlignment="Top"
                        Margin="8,0,0,0"
                        Padding="4,1"
                        BorderThickness="0"
                        Background="Transparent"
                        Foreground="#C62828"
                        FontWeight="Bold"/>
            </Grid>
        </Border>

    </StackPanel>
</Window>
